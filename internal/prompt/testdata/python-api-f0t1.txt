TASK_ID=F0.T1
You are executing task F0.T1.

Working dir: /tmp/orch-golden-project
Title: Scaffold FastAPI project layout
Description: Create pyproject.toml (dependencies: fastapi>=0.115,<0.116, uvicorn[standard]>=0.30; dev: pytest>=8.0, ruff>=0.6, httpx>=0.27). Add app/__init__.py, app/main.py exposing `app = FastAPI()`, and tests/__init__.py. Ensure `pip install -e .[dev]` completes.
Files you may write: ['pyproject.toml', 'app/__init__.py', 'app/main.py', 'tests/__init__.py']
Spec ref (READ FIRST): specs/f0-foundation.md#T1

Coordination protocol:
1. Read the spec ref for the exact acceptance criteria for F0.T1.
2. Do the work. If blocked, run: scripts/task-block.sh F0.T1 "<reason>" "claude/claude-sonnet-4-6" and STOP.
3. On success, run: scripts/task-finish.sh F0.T1 "<what you did>" "claude/claude-sonnet-4-6"
   Note: the orchestrator will call scripts/task-start.sh F0.T1 BEFORE launching you.

Constraints:
- Do NOT edit tasks.json directly.
- Do NOT touch files outside the list above unless the spec explicitly requires it.
- Report progress via the scripts above only.
